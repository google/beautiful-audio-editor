/**
 * @fileoverview Extern for typed arrays.
 * TODO(chizeng): Expand this extern to cover the whole interface.
 */

/**
 * A native typed array.
 * @constructor
 */
var TypedArray = function() {};


/**
 * The byte length of the array.
 * @type {number}
 */
TypedArray.prototype.byteLength;
